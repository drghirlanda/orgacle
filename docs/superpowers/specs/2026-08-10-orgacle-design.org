#+TITLE: Orgacle — design
#+AUTHOR: Stefano Ghirlanda
#+DATE: 2026-08-10
#+OPTIONS: toc:2 num:nil

Status: approved, pending implementation

Rework =epresent.el= (a private fork of =dakrone/epresent=) into =orgacle=, a
robust, modular, tested Org-mode presentation package fit for MELPA.

* 1. Context

=epresent.el= is a 1187-line single file. Upstream =dakrone/epresent= has been
dead since 2016; this fork carries 59 commits of new work added since 2020
(speaker notes, file/video display, fringe indicators, slide-in animation,
laser-pointer mouse, LaTeX/PDF export, speaking-time estimation).

The package works for its author but cannot be published as-is. Measured state
on Emacs 30.2:

- byte-compiler :: no =lexical-binding= cookie; ~40 free-variable assignments;
  12 obsolete Org APIs; 9 undefined functions.
- =package-lint= :: ~60 errors (dependency floor, symbol prefix, reserved keys).
- =checkdoc= :: ~80 docstring violations.
- tests :: none. CI: none.

* 2. Goals

1. A package that installs cleanly for a stranger and does not break their Emacs.
2. A codebase that can be changed safely — modular, tested, CI-verified.
3. The features that are half-finished today, finished.
4. Four new presentation capabilities.
5. Accepted into MELPA.

* 3. Non-goals

- HTML/reveal.js export. The LaTeX/PDF backend stays; nothing new is added.
- Backward compatibility with =EPRESENT_*= properties at runtime (see section 4.3).
- Supporting Emacs < 29.1.
- Merging back into =dakrone/epresent=.

* 4. Decisions

** 4.1 New package name: =orgacle=

MELPA's =epresent= recipe points at =dakrone/epresent=; MELPA does not accept a
fork under an existing package's name. =orgacle= is unclaimed on MELPA, GNU ELPA
and NonGNU ELPA.

Consequences: symbol prefix =orgacle-=, file =orgacle.el=, customization group
=orgacle=, properties =ORGACLE_*=, keywords =#+ORGACLE_*=. The GitHub repository
is renamed =drghirlanda/orgacle= (GitHub keeps a redirect from the old name).

Package header:

#+begin_src emacs-lisp :eval no
  ;;; orgacle.el --- Presentation mode for Org-mode  -*- lexical-binding: t; -*-
  ;; Maintainer: Stefano Ghirlanda <drghirlanda@gmail.com>
  ;; URL: https://github.com/drghirlanda/orgacle
  ;; Version: 2.0.0
  ;; Keywords: outlines, hypermedia, multimedia
  ;; Package-Requires: ((emacs "29.1") (org "9.6"))
#+end_src

Prior authors (Tromey, Hagelberg, Schulte, Chaganti, Hinman) stay in the
=Authors:= header; the Commentary states the package's origin as a fork of
epresent. Version starts at 2.0.0 to signal discontinuity from epresent 1.5.0.
A =COPYING= file carrying GPL-3 is added.

** 4.2 Floor: Emacs 29.1 / Org 9.6

Emacs 29.1 bundles Org 9.6, the first release with =org-fold-*=. Using it as the
floor means the fold and visibility APIs are called directly, with no shim.

One shim remains: =org-link-preview-*= (Org 9.8, bundled with Emacs 31) replaced
=org-redisplay-inline-images=, =org-remove-inline-images= and
=org-inline-image-overlays=. These are called via =orgacle--link-preview-refresh=
and =orgacle--link-preview-clear= in =orgacle-core.el=, dispatching on =fboundp=.

CI matrix: 29.4, 30.2, snapshot.

** 4.3 Properties: =ORGACLE_*= only, with a migration command

Old decks are converted, not supported forever. =orgacle-migrate-buffer= and
=orgacle-migrate-file= rewrite =:EPRESENT_X:= to =:ORGACLE_X:= and
=#+EPRESENT_X:= to =#+ORGACLE_X:=, report the number of substitutions, and
operate on the region when one is active. This keeps every property lookup a
single unconditional call and removes a whole class of "which name won" bugs.

All lookups still route through one accessor, =orgacle--prop=, so that the
property namespace is defined in exactly one place.

** 4.4 Optional dependencies

Nothing beyond Emacs and Org is a hard requirement.

| Dependency              | Today                                                                      | Target                                                                                                                                |
|-------------------------+----------------------------------------------------------------------------+---------------------------------------------------------------------------------------------------------------------------------------|
| =org-superstar=         | top-level =require= — *breaks install*, not declared in =Package-Requires= | =(require 'org-superstar nil t)=; =orgacle-use-org-superstar= defaults to t; absent package logs once and falls back to plain bullets |
| =pdf-tools=             | 6 functions called unguarded                                               | =declare-function= + =featurep= check; PDF display degrades to whatever mode Emacs picks                                              |
| =image-mode= transforms | 2 functions called unguarded                                               | =fboundp= guard                                                                                                                       |
| video player            | =mplayer= hardcoded default, =vlc= alternative                             | =executable-find= over =orgacle-video-players= (mpv, mplayer, vlc; mpv first — mplayer is effectively dead)                           |

Rationale for keeping =org-superstar= out of =Package-Requires=: it is purely
cosmetic, and a presentation package should not drag in a bullet-styling package
for users who do not want it. The cost is one =fboundp= branch.

* 5. Architecture

** 5.1 Files

#+begin_example
  orgacle-core.el       defgroup, faces, defcustoms, session state, orgacle--prop,
                        compat shims, overlay bookkeeping, error logging
  orgacle-nav.el        slide vector, page and subheading navigation      -> core
  orgacle-fontify.el    overlay fontification and hiding                  -> core
  orgacle-media.el      show-file (aux window, PDF, image), video,
                        fringe indicators                                 -> core
  orgacle-notes.el      notes collection, presenter view, talk timer      -> core, nav
  orgacle-reveal.el     incremental reveal                                -> core
  orgacle-appearance.el per-slide background and text scale               -> core
  orgacle.el            mode, keymap, orgacle-run / orgacle-quit,
                        autoloads                                         -> all above
  ox-orgacle.el         LaTeX/PDF export backend                          -> ox-latex
#+end_example

Dependencies point one way only: feature modules depend on =orgacle-core=, never
on each other; =orgacle.el= depends on everything. =ox-orgacle.el= is loadable on
its own without starting a presentation.

Each file must byte-compile standalone with =byte-compile-error-on-warn=.

** 5.2 The page hook — the only inter-module interface

Today =epresent-current-page= hard-codes calls into five subsystems, and
=epresent-show-file= reaches into fringe-overlay state owned by the indicator
code. That coupling is why the =SHOW_AUTO=/indicator interaction took four
attempts to get right.

Replace it with one extension point:

#+begin_src emacs-lisp :eval no
  (defvar orgacle-page-hook nil
    "Hook run after a slide has been displayed and narrowed.")
#+end_src

=orgacle-media=, =orgacle-notes=, =orgacle-reveal= and =orgacle-appearance= each
add a function at load time. =orgacle--display-page= is then:

1. tear down the previous slide's transient state (aux window, fringe overlays,
   reveal overlays, appearance remapping);
2. narrow to the slide's subtree and set visibility;
3. run =orgacle-page-hook=.

Users get the same hook as an extension point.

** 5.3 Error handling

*Principle: once a presentation is running, nothing may drop the presenter out
of it.*

- Every =orgacle-page-hook= member runs inside =condition-case=. A failure
  appends to =*Orgacle Log*= (with slide index and backtrace) and the remaining
  hook members still run.
- =orgacle-show-file= on a missing file currently calls =user-error= mid-talk.
  It logs and returns instead.
- =orgacle-quit= is idempotent and wrapped in =unwind-protect=, so a broken
  session state cannot strand the user in a fullscreen frame.
- Missing optional dependencies produce one clear message, never a backtrace.

** 5.4 Session state

~20 loose global =defvar= s are mutated ad hoc today, and =epresent-quit=
restores some Org variables by hand while unconditionally calling =(org-mode)=.

Replace with:

- =orgacle--session=, a =cl-defstruct= holding frame, source buffer, restriction,
  temp file, slide vector, index, aux window, notes buffer, timer start.
- =orgacle--saved-globals=, an alist of =(symbol . value)= captured by
  =orgacle--save-user-state= from one declared list of variables and replayed by
  =orgacle--restore-user-state=. Adding a variable to the list is then a one-line
  change that cannot be forgotten on the restore side.

=orgacle-quit= locates its frame from the struct, not by comparing the frame
title to the string ="EPresent"=.

* 6. The slide model

The highest-value structural change; P3 depends on it and P4 builds on it.

Navigation today is =org-get-next-sibling= plus a recursive =skip= flag used to
step over the title page, and =epresent-jump-to-page= calls =epresent-top= then
loops =epresent-next-page=, re-rendering and re-animating every intervening
slide.

Replace with a vector built once at =orgacle-run=:

#+begin_src emacs-lisp :eval no
  (orgacle--session-slides session)  ; vector of markers, one per real slide
  (orgacle--session-index  session)  ; current position
#+end_src

A heading is a slide when it is at frame level and is not a title page, not
tagged or propertied =ORGACLE_HIDE=, and not a speaker-notes subtree. Navigation
becomes index arithmetic against that vector.

Bugs this eliminates as a class:

- title-page skip recursion between =current-page=, =next-page= and
  =previous-page= (four commits fought this);
- page-number drift during backward navigation;
- flickering, animation-firing =jump-to-page=, now O(1);
- notes-buffer positioning by regexp search on heading text, which breaks on
  duplicate headings — becomes an index lookup into a parallel marker vector;
- "slide N of M", required by the presenter view, now free.

* 7. Defects to fix

Confirmed by byte-compilation and reading. Phase in brackets.

** Dead code that errors when called [P0]

- =epresent-increase-font= and =epresent-decrease-font= iterate over
  =epresent-content-face= and =epresent-fixed-face=. Neither face exists — the
  faces defined are title/heading/subheading/author/bullet/hidden. Both commands
  signal on invocation. Fix: operate on the faces that exist, and bind them to
  keys (they are currently unreachable from the keymap).
- =(setq org-hide-pretty-entities t)= in the mode body sets a variable that does
  not exist; the real one is =org-pretty-entities=, which is saved and restored
  but never actually changed. Pretty-entity handling has never worked.

** Invalid customization metadata [P0]

- =epresent-tooltip-mode= — =:type 'bool= is not a valid customization type, and
  the value is used as a 0/1 mode argument. Becomes =orgacle-tooltips=,
  =:type 'boolean=, passed as =(if orgacle-tooltips 1 -1)=.
- =epresent-mode-line= — =:type 'string= but the value is a mode-line construct.
  Becomes =:type 'sexp=.
- =epresent-speaker-notes= — =:type 'string= but the value is a boolean.
- =(condition-case ex ... ('error ...))= — quoted condition name.
- =mark-whole-buffer= called from Lisp in the export backend.

** Portability [P3]

- =x-pointer-shape=, =x-sensitive-text-pointer-shape= and =x-pointer-invisible=
  are X11-only. On pgtk, Wayland, macOS and Windows the laser-pointer code and
  =epresent-toggle-mouse= signal. Guard on =(boundp 'x-pointer-shape)= and
  degrade silently.

** Half-finished features [P3]

- =epresent-internal-border-width= is docstringed =NOT WORKING=: the defcustom
  exists but =make-frame= hardcodes =(internal-border-width . 75)=. Fix by using
  the variable, applying it with =set-frame-parameter= after creation so reused
  frames pick up changes, and re-testing the reported fringe interaction (commit
  =f9bc35e= removed the customization because it disturbed fringe indicators —
  with indicators redesigned, retest rather than assume).
- Indicator / =SHOW_AUTO= cross-talk. Indicators become derived state: computed
  from the slide's properties, drawn by one page-hook member, cleared by one
  teardown function. =show-file= no longer touches indicator overlays.
- =epresent-quit= kills the notes frame via
  =(window-frame (get-buffer-window epresent-notes-buffer))=, which signals when
  the buffer is not displayed.

* 8. New capabilities [P4]

All four build on the slide vector.

- *Presenter view.* Promotes the existing notes frame to a console in
  =*Orgacle Presenter*=: current slide's notes, next slide's title, =N/M=,
  elapsed time, remaining time. Refreshed from =orgacle-page-hook=.
- *Talk timer.* Starts at =orgacle-run=. Target duration from
  =#+ORGACLE_DURATION:= (minutes). Shown in the mode line and the presenter view;
  face shifts at 90% and 100% of target. Complements the existing
  word-count-based speaking-time estimate.
- *Incremental reveal.* Generalizes =EPRESENT_STEPWISE=. Reveal targets for the
  current slide (list items, subheadings, blocks — selected by
  =:ORGACLE_REVEAL:=) are collected into a list; state is an index into it, so
  forward and backward are symmetric. This replaces the current
  hide-subtree/show-subtree hack, which cannot step backwards correctly. =n= and
  =p= advance the reveal first and change slide once it is exhausted;
  configurable.
- *Per-slide appearance.* =:ORGACLE_BACKGROUND:= and =:ORGACLE_TEXT_SCALE:=
  applied through buffer-local =face-remapping-alist= and a frame parameter,
  reset on every page change so no slide leaks styling into the next.

* 9. Testing

ERT, batch-runnable, no window system needed for the bulk.

Covered in batch: =#+ORGACLE_*= keyword parsing; =orgacle--prop=; slide-vector
construction over fixtures (nested levels, title page, hidden slides, speaker
notes); navigation and page numbering, forwards and backwards; notes collection;
speaking-time estimate; migration command; the export backend's property-drawer
translator; the reveal state machine; state save/restore round-trip.

Not testable in batch — frame creation, fringe rendering, mouse pointer shape,
slide-in animation, video playback. These are isolated behind thin wrapper
functions so that the logic around them is testable, and covered by an optional
=xvfb-run= smoke job that starts and quits a presentation.

Fixtures in =test/fixtures/*.org=. =present.org= becomes the manual end-to-end
demo and is migrated to =ORGACLE_*=.

Build and CI: a =Makefile= driving four batch targets through a bootstrap file
that provisions development dependencies into =./.deps=, so a checkout needs
nothing on the machine but Emacs. GitHub Actions runs the same =make= targets
over a matrix of Emacs 29.4 / 30.2 / snapshot; targets are =compile= (with
=byte-compile-error-on-warn=), =checkdoc=, =package-lint= and =test=.

Eldev was the first choice and was rejected during P0 planning: it is not
installed here and not packaged for Slackware, and the dependency provisioning
it would have done is eight lines of =package-install=. Running the same
commands locally and in CI is worth more than the lines it would save.

* 10. Phases

Each phase ends green — compile, lint, checkdoc and tests all pass — and is
independently committable.

** P0 — Safety net and compliance

Makefile and test harness, ERT suite, CI workflow, fixtures. Characterization
tests for the logic that is extractable today. Then =lexical-binding: t= and
elimination of all ~40 free variables. Fix the invalid =defcustom= types and the
dead-code defects of section 7. Docstrings to checkdoc-clean. Remove the
load-time =(define-key org-mode-map [f5] ...)= — MELPA forbids it and the keys
are reserved for users; ship an autoloaded command and document the binding.
=Package-Requires= set to the 29.1/9.6 floor.

Turning on =lexical-binding= is a semantic change at every one of those ~40
sites, which is exactly why the characterization tests come first in this phase
and not later.

Also here, moved from P2 during planning: replacing the twelve obsolete Org API
calls and adding the =org-link-preview-*= shim, and making =org-superstar=
optional. The phase's exit criterion is a clean compile under
=byte-compile-error-on-warn=, and on the snapshot Emacs the obsolete calls are
warnings — therefore errors. P0 cannot be green without them. The undeclared
top-level =(require 'org-superstar)= is likewise an install-breaking defect, not
a tidiness matter, so it is fixed as soon as there is a test proving the package
loads without it.

The detailed task breakdown is
=docs/superpowers/plans/2026-08-11-p0-safety-net.org=, which also records nine
defects found while planning that are not listed in section 7 — among them that
=epresent-slide-in= has never been consulted, so the slide-in animation runs on
every slide, and that the LaTeX backend never sees its own property drawers
because =org-export-with-properties= defaults to nil.

*Exit:* byte-compile clean under =byte-compile-error-on-warn=; checkdoc clean;
=package-lint= clean; tests green on all three Emacs versions.

*Status: COMPLETE* (2026-08-11). All exit criteria met: =make clean && make all=
exits 0, and CI is green on Emacs 29.4, 30.2 and snapshot. The suite is 32
tests, up from none. Two tasks were inserted during the phase, both because CI
found something the plan had not anticipated — 3a (the package could not load on
any Emacs without X11) and 8a (a stale =.elc= could shadow freshly edited
source, so a suite run after an edit tested the previous version of the code).

Defects fixed in P0 beyond those section 7 anticipated: the =.org= include
branch of the LaTeX backend (which additionally could destroy a user's unsaved
buffer), =pdf-view-current-page= being called as a function when it is a macro,
=epresent-slide-in= never being consulted, the speaking-time estimate
truncating rather than rounding, the export backend never seeing its own
property drawers, and =epresent-hidden-face= carrying a =:invisible= attribute
that has never been a valid face attribute.

** P1 — Rename

Mechanical rename of symbols, file, group, faces, properties and keywords.
=orgacle-migrate-buffer= and =orgacle-migrate-file= with tests. README and
=present.org= updated.

Two steps in this phase are the maintainer's to perform, not automatable from
the working tree: renaming the GitHub repository to =drghirlanda/orgacle=
(GitHub leaves a redirect behind), and renaming the local checkout directory
from =epresent= to =orgacle=. Both are confirmed before P1 begins.

*Exit:* no =epresent= string outside the Commentary's origin note; tests green.

*Status: COMPLETE* (2026-08-12). =make clean && make all= exits 0, and CI has
been green on Emacs 29.4, 30.2 and snapshot
(https://github.com/drghirlanda/orgacle/actions/runs/31623164306).
=orgacle-migrate-buffer= and =orgacle-migrate-file= ship with tests and
=test/fixtures/legacy.org=; a manual round-trip against a copy of
=sub-level.org= (real =ORGACLE_FRAME_LEVEL= property, reverted with =sed= and
migrated back in batch) reproduced the committed file exactly.

The two gaps originally recorded here are both closed:

- =present.org= now names Orgacle throughout -- title, the "Source code" demo
  slide (=orgacle-run=, =orgacle-mode=, ="Orgacle can only be used..."=) -- and
  its example =#+RESULTS:= no longer hardcodes a path from a checkout that
  does not exist.
- The CI workflow's =push= trigger is filtered to =branches: [master]=;
  =pull_request= covers feature branches (including same-repo ones, so the
  three-Emacs matrix does not run twice per push), and =workflow_dispatch=
  remains as a manual fallback for a branch with no open pull request.

A final whole-branch review ahead of merge additionally found and fixed, all
on this branch:

- *Critical:* =orgacle-migrate-file= called =find-file-noselect=, which
  returns an already-open buffer as-is, narrowing included. Migrating a file
  open and narrowed in another buffer (e.g. via =org-narrow-to-subtree=, an
  everyday Org action) converted only the accessible portion while reporting
  the file fully migrated, silently leaving =EPRESENT_= properties on disk.
  Fixed by widening inside =orgacle-migrate-file= before delegating to
  =orgacle-migrate-buffer=, which correctly keeps respecting the caller's
  restriction as a buffer command. Regression-tested.
- =orgacle-migrate-buffer='s search ran under the buffer's default
  case-insensitive =case-fold-search=, so prose mentioning lowercase
  =epresent_...= was rewritten to uppercase =ORGACLE_...=. Fixed by binding
  =case-fold-search= to nil around the search loop.
- =orgacle-migrate-file= had no tests at all; two now cover it, including the
  narrowing regression above.
- The =Authors:= header was missing Phil Hagelberg and Puneeth Chaganti,
  both credited in the Commentary's origin note; added.
- The stale "works best in Emacs >= 24" sentence in the Commentary was
  dropped (the floor is Emacs 29.1).
- =README.org= now has a License section pointing at =COPYING=.

** P2 — Modularization and modernization

Split into the files of section 5.1. Introduce =orgacle-page-hook= and remove
sideways calls. Give =pdf-tools= and the image transforms runtime handling, so a
PDF slide degrades gracefully rather than landing in whatever mode Emacs picks;
detect the video player with =executable-find=. Introduce =orgacle--session= and
the save/restore mechanism; make =orgacle-quit= idempotent.

The obsolete-API replacement, the =org-link-preview-*= shim and the
=org-superstar= softening moved to P0; see there.

*Exit:* each file byte-compiles standalone; tests green.

*Status: COMPLETE* (2026-08-12). =make clean && make all= exits 0, and CI is
green on Emacs 29.4, 30.2 and snapshot
(https://github.com/drghirlanda/orgacle/actions/runs/31646009314). The
1371-line =orgacle.el= of 2026-08-12 became eight files totalling 1686 lines:
=orgacle-core.el= 434 (the leaf: customization surface, session state,
compatibility shims), =orgacle-nav.el= 136, =orgacle-fontify.el= 192,
=orgacle-src.el= 93, =orgacle-media.el= 248, =orgacle-notes.el= 108,
=ox-orgacle.el= 118 (loads standalone, no =orgacle-core= dependency), and
=orgacle.el= itself down to 357 (mode, keymaps, =orgacle-quit=, =orgacle-run=,
migration commands). Every feature module requires only =orgacle-core=, never
a sibling; =orgacle-page-hook= replaced the sideways calls
=orgacle-current-page= used to make into every subsystem. The suite grew from
40 tests (the P1 baseline) to 48.

=orgacle--session= did not ship under that name: the equivalent is
=orgacle-saved-variables=, a =defconst= list of the Org-mode variables the mode
changes while presenting, read by =orgacle--save-user-state= and
=orgacle--restore-user-state=. It closed a real bug found along the way --
=orgacle-mode= set =org-fontify-quote-and-verse-blocks= and never restored it,
so quitting a presentation left it permanently changed in the user's Emacs.
=orgacle-quit= is now idempotent (calling it twice signals nothing), and
=orgacle-mode= no longer crashes on a byte-compiled build whose
=standard-display-table= has never been created -- both found and fixed
during the phase, neither anticipated by the plan.

=pdf-tools= and =image-mode= now degrade at runtime instead of assuming
presence: a PDF opens unfitted with a one-time message when =pdf-tools= is
absent, rather than however Emacs happens to display it. The video player is
resolved with =executable-find= before the frame ever leaves full screen, so
an unsupported player name or a missing binary is reported as a =user-error=
instead of leaving the presentation stuck.

This task's own verification found and fixed a defect the phase had carried
since Task 1: =orgacle.el='s =orgacle-quit= sets two X11 pointer-shape
variables that only =orgacle-core.el= declared (with a value-less =defvar=,
which -- unlike one with a value -- only informs the byte-compiler within the
file it appears in, not files that merely require it). Every local build
throughout Tasks 1-7 ran on an X11-capable Emacs, where these variables are
already special before any of this package's code runs, so the missing
declaration was invisible locally; =orgacle-modules= had also never been
pushed before this task, so CI -- a headless build, where nothing pre-declares
them -- never ran against this branch until now and caught it on the first
try. Fixed by redeclaring the two variables =orgacle.el= actually sets, the
same pattern already used for =flyspell-mode-off=.

** P3 — Slide model and robustness

Build the slide vector; rewrite navigation as index arithmetic; O(1) jump.
Reposition notes by index. Fix the section 7 portability and
half-finished-feature defects. Install the presentation-safe error handling of
section 5.3.

*Exit:* navigation tests cover nested, hidden, title-page and speaker-notes
fixtures forwards and backwards; no unguarded X11 call remains.

*Status: COMPLETE* (2026-08-13). =make clean && make all= exits 0, and CI is
green on Emacs 29.4, 30.2 and snapshot
(https://github.com/drghirlanda/orgacle/actions/runs/31687297731). The suite
grew from 51 tests (the P2 baseline on this branch) to 83.

The section 6 vector shipped as designed: =orgacle--session-slides=, a vector
of markers built once by =orgacle--build-slides= and walked by index
(=orgacle--session-index=), with =orgacle--goto-slide= as the one shared,
clamping entry point behind =orgacle-top=, =orgacle-next-page=,
=orgacle-previous-page= and =orgacle-jump-to-page=. This retired three real
bug classes the old code carried, confirmed by reading it before deletion:
=orgacle-jump-to-page= was =(orgacle-top)= followed by =(orgacle-next-page)=
called =(1- num)= times, re-rendering and re-animating every intervening
slide instead of just the one requested, an O(N) walk
=orgacle-test-jump-is-direct= now pins at O(1) by counting page-hook
invocations; the title-page skip was mutual recursion
between =orgacle-current-page=, =orgacle-next-page= and =orgacle-previous-page=
threading a =backward= or =skip= flag through each other (section 6: "four
commits fought this"); and =orgacle-page-number= was a counter separately
=cl-incf='d and =cl-decf='d alongside the actual outline position, free to
drift out of sync with it, rather than a value derived from the index the way
=orgacle--sync-page-number= derives it now. The speaker-notes buffer is
repositioned by =orgacle--session-notes-markers=, a parallel vector recorded
at construction time (Task 3), replacing a regexp search for the current
heading's text that matched the *first* heading of that title in the buffer,
silently showing the wrong slide's notes whenever two slides shared a title.

=orgacle--session=, a =cl-defstruct= (=orgacle-core.el=), now holds the
running presentation's entire lifetime state in ten slots -- =frame=,
=org-buffer=, =org-restriction=, =org-file=, =slides=, =index=,
=notes-buffer=, =notes-markers=, =aux-window=, =presentation-window= --
replacing the loose =defvar= forms P2 still had. =orgacle-quit= finds its own
presentation frame from the session's =frame= slot, checked with
=frame-live-p=, rather than comparing the =title= parameter of whatever frame
happened to be selected when =q= was pressed against the literal string
"Orgacle", which is what P2's =orgacle-quit= still did. It now restores only
what it actually saved -- X pointer shape,
=x-sensitive-text-pointer-shape=, =void-text-area-pointer=, =tooltip-mode=, and
the display-table's =selective-display= slot -- each guarded by the same
=orgacle--saved-state= re-entrancy flag, rather than unconditionally
overwriting them with the mode's own defaults when =orgacle-quit= is called
with no presentation running. =*Orgacle Log*= records a timestamp, the
presenter-facing (one-based) slide number, the failing page-hook member and
the error, is wrapped in =ignore-errors= so the logger itself cannot abort the
hook it is logging for, and is never displayed -- confirmed in a real frame
this task: a deliberately failing hook member logged correctly while the
slide stayed on screen and no window ever showed the log buffer. The mouse
toggle (=m=) now actually alternates instead of only ever hiding, and
=orgacle-internal-border-width= is wired into =make-frame='s initial parameter
alist in place of the old hardcoded =75=, applied once at frame-creation time
rather than via a later =set-frame-parameter= on an already-live frame -- a
deliberate departure from this section's original proposal, chosen because
applying it post-creation is exactly what commit =f9bc35e= did before it
disturbed the fringe indicators and got reverted; baking it into frame
creation avoids that failure mode entirely, at the cost of the value only
taking effect on the next =orgacle-run=, which the docstring now says
explicitly. Every X pointer-shape read or write in the package is guarded by
=(boundp 'x-pointer-shape)=, confirmed both by a dedicated test that unbinds
the X pointer constants and reloads the package (=orgacle-test-loads-without-x11=)
and, this task, by CI itself: the first push of this branch failed identically
on all three Emacs versions because =orgacle--get-frame= reset
=orgacle-mouse-visible= to =t= *inside* that guard, so on GitHub Actions'
headless runners -- where =x-pointer-shape= is genuinely unbound, unlike every
local run in this phase -- the reset never ran; moved outside the guard,
confirmed both ways locally, and CI went green on the next run.

Verified in a real, rendered frame (Xvfb plus a window manager, driven with
real key events, not =--batch=) rather than only in ERT, since batch Emacs
cannot open a window and every phase before this one had only ever been
checked that way: navigation forwards and backwards, the mode line, the mouse
toggle (pointer shape variables checked directly, not just the bookkeeping
variable), showing a file with =i=, the pointer/tooltip-mode/display-table
restore on quit against sentinel values distinct from every default in the
system, and the border width visibly taking effect. One outstanding gap
surfaced this way that batch testing could not have shown: =orgacle-run= still
never calls =orgacle-top= (or any equivalent), so immediately after running it
the buffer is fully widened, not narrowed to the first slide -- on
=present.org= this is masked by coincidence, since the file's front matter and
first heading happen to render as a plausible-looking first screen, but
scrolling from there reveals the rest of the outline underneath, and a second
=orgacle-run= in the same buffer (after a =q= then re-run) shows it plainly,
since point is left wherever the previous session's last navigation put it
rather than being reset. Flagged by Task 2's review as pre-existing and out of
scope for that task; still true, still out of scope for this one, and now
confirmed with real-frame evidence rather than only inferred from reading the
function.

** P4 — New capabilities

The four features of section 8, each with tests for its state machine.

*Exit:* tests green; each feature demonstrated in =present.org=.

*Status: COMPLETE* (2026-08-14). All exit criteria met locally: =make clean &&
make all= exits 0 on Emacs 30.2, and all four capabilities are demonstrated in
=present.org=. The suite grew from 89 tests (this phase's own starting count,
after P0-P3 were merged to master and this branch was cut) to 199, plus three
expected failures — the pre-existing, documented =Speaker notes=/=ORGACLE_HIDE=
notes-leak issue below, pinned with =:expected-result= rather than left to fail
silently. Two new files joined the five from P2, =orgacle-reveal.el= and
=orgacle-appearance.el=, for ten total; section 5.1's dependency rule held
throughout — every feature module depends only on =orgacle-core=, never on a
sibling, confirmed this phase's final task by compiling each of the ten files
alone in a fresh Emacs rather than only in the Makefile's own fixed sequence,
which could have masked a module that only compiled because a sibling happened
to load first.

Each of the four capabilities went in behind the phase's own standing
constraint — off by default, costing nothing unused, applied state undone on
leaving the slide — with two controller rulings narrowing what "off by
default" actually requires. Task 1's talk timer shows nothing at all, not even
a bare elapsed clock, until a target duration is configured via
=#+ORGACLE_DURATION:= or =orgacle-duration=: elapsed-with-no-target was ruled
not actionable, and the audience-facing mode line must look exactly as it did
before the timer existed for a presenter who configured nothing. Task 2's
presenter console — =orgacle-presenter-view=, default =t= — was ruled the one
deliberate exception: it lives entirely in the =*Orgacle Notes*= buffer's own
=header-line-format=, a presenter-private surface already opted into via
=orgacle-speaker-notes=, never in that buffer's text and never on the projected
frame, so defaulting it on does not touch anything an audience sees. That
buffer is also where section 8's design deviated from the plan as written: the
console is the existing notes buffer promoted in place, not a new
=*Orgacle Presenter*= buffer, because a =header-line-format= of =(:eval ...)=
is re-evaluated on every redisplay regardless, so =redraw-display= keeps the
console in step with navigation with no page-hook member of its own to
maintain. Task 3's incremental reveal generalizes =EPRESENT_STEPWISE= into a
symmetric index over a slide's =:ORGACLE_REVEAL:= targets (=items=,
=headings=, or =blocks=), so backward steps correctly un-reveal instead of
collapsing the whole subtree as the old hide/show-subtree hack did; =n= and
=p= drive it first and only change slide once it is exhausted, governed by
=orgacle-reveal-on-navigation= (default =t=). Task 4's per-slide appearance
applies =:ORGACLE_BACKGROUND:= and =:ORGACLE_TEXT_SCALE:= through a
buffer-local =face-remapping-alist= and the frame's own background parameter,
reset on every page change so nothing leaks to the next slide; the task also
found and fixed a real-frame-only defect no batch test could have shown, a
100%-white right fringe on a coloured slide because =orgacle--get-frame= pins
the fringe face at frame-creation time while the appearance code only touched
the frame parameter, caught by pixel-sampling a live Xvfb frame rather than by
reading the code. Task 6 closed four items P3 had deferred: the dead
=orgacle-goto-top-level= command was removed; =orgacle-refresh= now re-narrows
in place, without running the page hook, by comparing the current restriction's
own bounds against the target slide's recomputed subtree bounds rather than
=buffer-narrowed-p= (itself replacing an even earlier position-only
comparison, across three fix rounds); a second =orgacle-run= now confirms and
tears down a genuinely live previous session instead of orphaning its frame;
and exiting =org-edit-src-code= now rebuilds a live notes buffer.

Task 7 verified the phase rather than building anything new. Beyond the
standalone-compile matrix above: the off-by-default constraint was confirmed
end to end, not per feature — a deck with none of the new properties, run for
real through =orgacle-run=, left a user's own customized =orgacle-mode-line=
byte-for-byte untouched, left the notes buffer's text exactly as P3 produced
it (the presenter-console header stayed in =header-line-format=, never in the
text), created no reveal overlays, and left =face-remapping-alist= nil. With no
=xvfb-run= on this machine, a real frame was driven the same way P3 and this
phase's earlier tasks did — =Xvfb= plus =fluxbox=, real =XTEST= key events via
=xdotool=, screenshots via =import= — exercising all four capabilities
together in =present.org=: the timer at =0:00/10:00= advancing live, its
=orgacle-timer-warning-face= and =orgacle-timer-overtime-face= colours
confirmed by pixel, not just by face symbol, at 90% and past 100% of target;
the presenter console tracking position, next-slide title and timer across
navigation; both reveal kinds (=items= on Introduction, =headings= on Agenda)
stepping forward via =N= and via plain =n=; the =Questions?= slide's yellow
background and enlarged text applying and resetting cleanly, its fringe
pixel-sampled to match the slide body in both directions with no white stripe;
and =orgacle-quit= restoring the presented buffer to a clean, unnarrowed,
unmodified state. One tooling artifact surfaced and is worth recording here so
it is not mistaken for a product defect later: this sandbox's =Xvfb=/=XTEST=
combination occasionally redelivered a key's autorepeat to whatever frame next
had focus after the original frame was destroyed (=orgacle-quit= deletes the
presentation frame mid-keystroke), self-inserting stray characters into
whatever buffer that frame showed; reproduced twice, traced to the input
layer rather than to Orgacle (only the literal key pressed ever reappeared,
never through any Orgacle code path that self-inserts), and confirmed harmless
— nothing was ever saved, =git diff= stayed empty throughout.

Five items were deliberately left for the final whole-branch review rather
than fixed here, and this task re-confirmed each still reproduces:
=orgacle-refresh='s docstring claim that its bounds predicate is "a strict
superset" of the =buffer-narrowed-p=-based disjunct it replaced does not hold
in either direction — confirmed empirically, not just by reading, with one
case each way; the README frames the notes-leak exemption as specific to
=Title page=, when =orgacle--slide-p= actually exempts all three hidden kinds
uniformly by frame level, so the project's own =* Hidden slide= fixture is
flagged as at-risk when it is not; the no-transition test at
=test/orgacle-test.el= around line 1172 carries a =#+begin_src= block its body
never executes, asserts point and reveal index but not hook count or
restriction bounds, and moves point with an unexplained =(forward-char 13)=;
=orgacle-run='s own docstring rationale for skipping a dead buffer restore
overstates what happens next — =find-file= only runs for a narrowed-region
presentation, not unconditionally; and the notes-buffer round-trip test passes
only because =orgacle--get-frame= is stubbed out — confirmed directly that the
unstubbed path fails hard under =-Q --batch= trying to create a real frame.
Full detail, reproductions and pixel/face evidence for all of the above are in
=.superpowers/sdd/2026-08-14-p4-capabilities.org/task-7-report.md=.

Per this task's own constraint, nothing was pushed and CI has not yet run on
this branch's final state; that confirmation on Emacs 29.4, 30.2 and snapshot
is the controller's to authorize.

** P5 — Ship

Fill the seven empty README sections (The basics, Inline images, Code blocks,
Indicators, Speaker notes, Slide-in effect, 'Laser pointer' effect). Screenshots
and a short screen recording. CHANGELOG, version 2.0.0. Submit the MELPA recipe
PR.

*Exit:* recipe merged.

* 11. Risks

- *=lexical-binding= surfaces latent behaviour.* Several of the ~40 free
  variables are read across function boundaries and currently work only through
  dynamic scope — =show-file= in the indicator code and =slide-local= in the
  slide-in code are the clearest cases. Mitigation: characterization tests
  before the switch; convert one function at a time.
- *The fringe/internal-border interaction may be genuinely unfixable.* Commit
  =f9bc35e= removed the border customization because of it. If retesting after
  the indicator redesign still shows a conflict, the fallback is to document the
  constraint and keep the border fixed, rather than ship a variable that lies.
- *MELPA review may object to the fork lineage.* Mitigation: the Commentary
  states the origin explicitly, authorship is preserved, and the name does not
  collide.
- *Uncommitted work in the tree.* =epresent.el=, =README.org= and =present.org=
  currently have uncommitted modifications, and =ltximg/= is untracked
  (=.gitignore= still lists Org's old =ltxpng= directory name). P0 starts by
  committing or discarding these and correcting =.gitignore=, so that the
  baseline is clean before any rewrite.
